import './globals.css';
export const metadata={title:'DeckBot Local',description:'MVP local para gerar apresentações comerciais em PDF'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="pt-BR"><body>{children}</body></html>}
