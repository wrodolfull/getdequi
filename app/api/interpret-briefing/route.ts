import { NextResponse } from "next/server";import { openai, openaiModel, parseJson } from "@/lib/openai";import { INTERPRETATION_PROMPT } from "@/lib/prompts";import { heuristicInterpretation } from "@/lib/deck";import type { BriefingInterpretation } from "@/lib/types";
export async function POST(req: Request){const {briefing}=await req.json(); if(!process.env.OPENAI_API_KEY) return NextResponse.json({interpretation:heuristicInterpretation(briefing)}); const r=await openai.chat.completions.create({model:openaiModel,messages:[{role:"system",content:INTERPRETATION_PROMPT},{role:"user",content:briefing}],response_format:{type:"json_object"}}); return NextResponse.json({interpretation:parseJson<BriefingInterpretation>(r.choices[0].message.content||"{}")});}
